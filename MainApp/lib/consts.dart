import 'package:flutter/cupertino.dart';

const appTitle = 'ToDo-List';
const defaultTextStyle = TextStyle(fontSize: 20);
const defaultPadding = EdgeInsets.fromLTRB(8, 8, 0, 0);
const defaultPaddingAll = EdgeInsets.all(20);